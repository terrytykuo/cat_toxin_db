{
  "name": "cat_toxin_db",
  "private": true,
  "version": "0.1.0",
  "dependencies": {
    "dotenv": "^16.6.1",
    "firebase": "^12.6.0"
  },
  "scripts": {
    "build:schemas": "npm --prefix schemas run build",
    "check:schemas": "npm --prefix schemas run check"
  }
}
