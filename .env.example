# Repo-level config. Copy to .env.local (gitignored), or provide the same key
# as a shell environment variable when starting the admin UI.

# Firebase Storage bucket name, e.g. catsafe-xxxxx.firebasestorage.app
# Read by admin/server.js on startup.
FIREBASE_STORAGE_BUCKET=your-bucket.firebasestorage.app
